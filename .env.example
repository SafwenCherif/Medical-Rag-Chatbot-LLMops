# Prefer Groq (free tier) — get a key at https://console.groq.com/keys
GROQ_API_KEY=

# Optional fallback — OpenRouter (OpenAI-compatible)
OPENROUTER_API_KEY=
OPENROUTER_MODEL=meta-llama/llama-3.1-8b-instruct

# Optional: HuggingFace (only needed if you switch embeddings/LLM hosting)
HF_TOKEN=
